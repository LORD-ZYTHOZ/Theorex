{
  "mcpServers": {
    "theorex": {
      "type": "stdio",
      "command": "bun",
      "args": ["run", "src/cli/index.ts", "mcp-start", "--stdio"],
      "cwd": "${workspaceFolder}",
      "description": "Theorex — semantic memory + Theronexus structural code intelligence"
    }
  }
}
