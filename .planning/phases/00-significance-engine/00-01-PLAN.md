---
phase: 00-significance-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/spike/alias-api.test.ts
autonomous: true
requirements:
  - SIG-03

must_haves:
  truths:
    - "compromise v14 nlp.extend() alias API is verified working or a documented workaround is confirmed"
    - "A test registers one abbreviation alias ('ml' → 'machine learning') and verifies canonical form collapse"
    - "The verified API pattern is recorded in a comment at the top of the test file for Plan 04 to reference"
  artifacts:
    - path: "src/spike/alias-api.test.ts"
      provides: "Proof of compromise v14 alias registration API shape"
      contains: "nlp.extend"
  key_links:
    - from: "src/spike/alias-api.test.ts"
      to: "src/synonyms.ts (Plan 04)"
      via: "nlp.extend() pattern documented in spike"
      pattern: "nlp\\.extend"
---

<objective>
Spike: verify the compromise v14 nlp.extend() alias registration API before synonyms.ts is written.

Purpose: Research flagged MEDIUM-LOW confidence on the exact nlp.extend() plugin API for registering word aliases in compromise v14.15.0. Writing synonyms.ts (Plan 04) against an unverified API risks a hard-to-debug runtime failure. This spike tests the API shape first.

Output: A passing test that registers one alias ('ml' → canonical 'machine learning') and confirms the canonical form is produced after normalization. The verified pattern is the blueprint for src/synonyms.ts.
</objective>

<execution_context>
@/Users/eoh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eoh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-significance-engine/00-CONTEXT.md
@.planning/phases/00-significance-engine/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Bun project and run compromise alias spike</name>
  <files>
    package.json
    tsconfig.json
    src/spike/alias-api.test.ts
  </files>
  <action>
    Initialize the Bun project and run the spike test.

    1. In /Users/eoh/theorex/, run:
       ```
       bun init -y
       bun add compromise
       bun add -d @types/bun
       ```

    2. Create tsconfig.json:
       ```json
       {
         "compilerOptions": {
           "target": "ESNext",
           "lib": ["ESNext"],
           "module": "Preserve",
           "moduleResolution": "bundler",
           "allowImportingTsExtensions": true,
           "verbatimModuleSyntax": true,
           "strict": true,
           "noFallthroughCasesInSwitch": true,
           "noUncheckedIndexedAccess": true,
           "noImplicitOverride": true
         }
       }
       ```

    3. Create src/spike/alias-api.test.ts. This file has TWO purposes:
       a) Prove the correct nlp.extend() pattern for v14.15.0
       b) Document the verified pattern in a comment block at the top for Plan 04 to copy

       The test must try the following strategies IN ORDER, asserting which one works:

       Strategy A — nlp.extend() with tag map:
       ```typescript
       nlp.extend((_Doc: unknown, world: { addWords: (w: Record<string, string[]>) => void }) => {
         world.addWords({ ml: ["Acronym"] });
       });
       ```

       Strategy B — nlp.world().addWords() direct:
       ```typescript
       (nlp as unknown as { world: () => { addWords: (w: Record<string, string[]>) => void } })
         .world()
         .addWords({ ml: ["Acronym"] });
       ```

       Strategy C — nlp.extend() with a lexicon object (v14 plugin API):
       ```typescript
       nlp.extend({ words: { ml: "Acronym" } });
       ```

       For EACH strategy, the test should:
       - Register 'ml' as an Acronym
       - Parse the text "ML is a field of ai"
       - Call .normalize({ acronyms: true }) on the result
       - Check whether the canonical form equals "machine learning" (after resolving ALIASES) OR whether "ML" is tagged as Acronym in .json()
       - The test PASSES if ANY strategy succeeds and produces a consistent Acronym tag for 'ml'
       - The test captures which strategy worked and records it as a console.log at the end

       IMPORTANT: The test does NOT need full synonym expansion (that is in synonyms.ts). It only needs to confirm that the nlp.extend() hook can register a word-to-tag mapping so that 'ml' in text receives the 'Acronym' tag.

       After running the test, add a comment block at the top of the file:
       ```
       // SPIKE RESULT: [STRATEGY X WORKS / ALL FAILED — FALLBACK REQUIRED]
       // Verified API: [copy the working code snippet here]
       // Used in: src/synonyms.ts registerAliases()
       ```

    4. Update package.json to add a "test" script: `"test": "bun test"`

    NOTE: The spike is DISCOVERY, not production code. It lives in src/spike/ and is never imported by production modules. If all three strategies fail, record that finding and propose the dictionary-only fallback (resolveAlias() without nlp.extend() — just a Map lookup before hashing, no compromise integration).
  </action>
  <verify>
    <automated>cd /Users/eoh/theorex && bun test src/spike/alias-api.test.ts</automated>
  </verify>
  <done>
    - bun test src/spike/alias-api.test.ts exits 0
    - Top of alias-api.test.ts contains a SPIKE RESULT comment documenting the working API pattern
    - Either a working nlp.extend() pattern is confirmed OR a fallback strategy is documented
  </done>
</task>

</tasks>

<verification>
bun test src/spike/alias-api.test.ts exits 0 with the spike result comment present at top of file.
</verification>

<success_criteria>
- Bun project initialized with compromise installed
- Spike test passes
- nlp.extend() API pattern for v14.15.0 is documented (or fallback confirmed)
- Plan 04 (identify + synonyms) can proceed with zero ambiguity about the alias API
</success_criteria>

<output>
After completion, create `.planning/phases/00-significance-engine/00-01-SUMMARY.md`
</output>
