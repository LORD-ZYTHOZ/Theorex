---
phase: 00-significance-engine
plan: 04
type: tdd
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - src/synonyms.ts
  - src/identify.ts
  - tests/synonyms.test.ts
  - tests/identify.test.ts
autonomous: true
requirements:
  - SIG-03

must_haves:
  truths:
    - "Same canonicalForm always produces the same conceptId across calls — deterministic hash"
    - "Two different canonical forms never produce the same conceptId in any test input set"
    - "Known abbreviation synonyms ('ml', 'ai', 'nlp', 'llm', 'rag') resolve to their expanded canonical form before hashing"
    - "resolveAlias() is pure — no side effects, same input → same output"
    - "registerAliases() uses the verified API pattern from the Plan 01 spike (SPIKE RESULT comment)"
    - "conceptId is a safe JavaScript Number (no BigInt precision loss for test inputs)"
  artifacts:
    - path: "src/synonyms.ts"
      provides: "ALIASES dictionary, resolveAlias(), registerAliases()"
      exports: ["resolveAlias", "registerAliases", "ALIASES"]
    - path: "src/identify.ts"
      provides: "assignIds(): readonly NormalizedConcept[] → readonly IdentifiedConcept[]"
      exports: ["assignIds"]
    - path: "tests/synonyms.test.ts"
      provides: "TDD tests for alias resolution"
    - path: "tests/identify.test.ts"
      provides: "TDD tests for ID assignment determinism"
  key_links:
    - from: "src/identify.ts"
      to: "src/synonyms.ts"
      via: "resolveAlias() called on canonicalForm before hashing"
      pattern: "resolveAlias"
    - from: "src/identify.ts"
      to: "Bun.hash.wyhash"
      via: "Number(Bun.hash.wyhash(resolvedForm, 0n))"
      pattern: "Bun\\.hash\\.wyhash"
---

<objective>
TDD implementation of synonym resolution and stable numeric ID assignment — pipeline steps 3 and 3a.

Purpose: assignIds() must produce byte-identical concept IDs across calls (purity). Synonym collapse is the only mechanism that ensures "ML" and "machine learning" map to one node — critical for the graph to function. The spike from Plan 01 determines which nlp.extend() API to use in registerAliases().

Output: synonyms.ts (alias dictionary + resolver) and identify.ts (deterministic ID hashing) with full test coverage. Must read the SPIKE RESULT comment from src/spike/alias-api.test.ts before implementing registerAliases().
</objective>

<execution_context>
@/Users/eoh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eoh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-significance-engine/00-CONTEXT.md
@.planning/phases/00-significance-engine/00-RESEARCH.md
@.planning/phases/00-significance-engine/00-01-SUMMARY.md
@.planning/phases/00-significance-engine/00-02-SUMMARY.md

<interfaces>
<!-- From src/types.ts (Plan 02) -->
```typescript
export interface NormalizedConcept extends RawConcept {
  readonly canonicalForm: string;
}

export interface IdentifiedConcept extends NormalizedConcept {
  readonly conceptId: number;
  // ID precision: Bun.hash.wyhash() → BigInt → Number
  // Safe for up to ~10M unique concepts (2^53-1 safe integer limit)
}
```
</interfaces>
</context>

<feature>
  <name>resolveAlias + assignIds</name>
  <files>src/synonyms.ts, src/identify.ts, tests/synonyms.test.ts, tests/identify.test.ts</files>
  <behavior>
    resolveAlias cases:
    - input: "ml" → output: "machine learning"
    - input: "ai" → output: "artificial intelligence"
    - input: "nlp" → output: "natural language processing"
    - input: "llm" → output: "large language model"
    - input: "rag" → output: "retrieval augmented generation"
    - input: "typescript" → output: "typescript" (no alias — return unchanged)
    - input: "ML" → output: "machine learning" (case-insensitive lookup)
    - input: "" → output: "" (empty string → empty string, no crash)

    assignIds cases:
    - input: NormalizedConcept with canonicalForm "typescript" → output: IdentifiedConcept with conceptId > 0
    - input: same canonicalForm twice → same conceptId (deterministic)
    - input: two different canonicalForms → different conceptIds (no collision for test inputs)
    - input: canonicalForm "ml" → conceptId === conceptId of "machine learning" (alias collapse before hash)
    - input: empty array → output: empty array
    - input: NormalizedConcept array → output spreads all fields + adds conceptId (immutable — no mutation)
    - all output conceptId values are finite safe integers (Number.isFinite and <= Number.MAX_SAFE_INTEGER)

    registerAliases cases:
    - calling registerAliases() twice is safe (idempotent — no duplicate registration error)
  </behavior>
  <implementation>
    BEFORE writing any code, read src/spike/alias-api.test.ts and find the "SPIKE RESULT" comment at the top.
    Use the verified API pattern for registerAliases(). If the spike recorded "ALL FAILED — FALLBACK REQUIRED",
    implement registerAliases() as a no-op and rely solely on resolveAlias() for synonym collapse.

    src/synonyms.ts:
    - ALIASES is a Record<string, string>: { "ml": "machine learning", "ai": "artificial intelligence", "nlp": "natural language processing", "llm": "large language model", "rag": "retrieval augmented generation" }
    - resolveAlias(canonical: string): string — looks up canonical.toLowerCase() in ALIASES, returns expanded form or input unchanged
    - registerAliases(): void — uses the spike-verified nlp.extend() pattern to register each alias key as an Acronym tag (or no-op if spike found no working API)
    - Call registerAliases() once at module load time using a top-level side-effect-free initialization (IIFE or module-level call). Note: this is a one-time registration against compromise's internal state — acceptable as a module initialization side effect.

    src/identify.ts:
    - Import { NormalizedConcept, IdentifiedConcept } from "../src/types.ts"
    - Import { resolveAlias } from "./synonyms.ts"
    - assignIds(concepts: readonly NormalizedConcept[]): readonly IdentifiedConcept[]
    - For each concept: resolved = resolveAlias(concept.canonicalForm)
    - conceptId = Number(Bun.hash.wyhash(resolved, 0n))
      — explicit seed 0n is REQUIRED for determinism (without seed, wyhash uses a random seed per process start)
    - Spread concept into new object + add conceptId (never mutate input)
    - File must stay under 50 lines

    TDD cycle:
    RED: Write tests/synonyms.test.ts + tests/identify.test.ts (fail — implementations don't exist yet)
    Commit: "test(00-04): add failing tests for resolveAlias and assignIds"
    GREEN: Write src/synonyms.ts, run tests — pass. Write src/identify.ts, run tests — pass.
    Commit: "feat(00-04): implement resolveAlias, registerAliases, assignIds"
  </implementation>
</feature>

<verification>
<automated>cd /Users/eoh/theorex && bun test tests/synonyms.test.ts tests/identify.test.ts --coverage 2>&1 | tail -20</automated>
</verification>

<success_criteria>
- bun test tests/synonyms.test.ts tests/identify.test.ts exits 0
- Same canonicalForm produces same conceptId in 3 independent calls (determinism test)
- "ml" and "machine learning" produce the same conceptId
- All output conceptIds are safe integers (Number.isFinite + <= Number.MAX_SAFE_INTEGER)
- registerAliases() is idempotent (called twice — no error)
- At least 80% line coverage
</success_criteria>

<output>
After completion, create `.planning/phases/00-significance-engine/00-04-SUMMARY.md`
</output>
